{% extends 'simplebase.html.twig' %}

{% block title %}{{ 'user.edit.title'|trans }}{% endblock %}

{% block simplebase %}

<div class="row justify-content-center text-center">
    <div class="col-12">
        <h1 class="mb-3">{{ 'user.edit.title'|trans }}</h1>
    </div>

    <div class="col-12">

        <a href="{{ path('user_account') }}">{{ 'user.account.title'|trans }}</a>

        {{ include('user/_form.html.twig') }}

    </div>

    <div>{{ include('user/_delete_form.html.twig') }}</div>

</div>

{% endblock %}