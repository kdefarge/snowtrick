{{ form_start(form_resetpassword, {'action': target_route}) }}
<div class="row justify-content-center">
    <div class="col-12">
        <h2 class="mb-3 text-center">{{ 'form.resetpassword.reset.title'|trans }}</h2>
    </div>

    <div class="col-10 col-sm-8 col-md-7 col-lg-5 border text-center bg-light py-3 px-sm-5">
    
        {{ form_row(form_resetpassword.plainPassword) }}
        
        <button class="btn btn-primary">{{ 'form.resetpassword.reset.button'|trans }}</button>
        
    </div>
</div>
{{ form_end(form_resetpassword) }}
